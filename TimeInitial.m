%% Initials
GeneralInitials
stepsize =0.25;
time_init=[minTime:1: maxTime];
StepSizeVector=zeros(size(time_init));
TimeVector1=zeros(size(time_init));
c0_init=zeros(size(time_init));
y0_update_init=c0_init;
